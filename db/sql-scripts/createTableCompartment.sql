CREATE TABLE compartment(
                       id INT AUTO_INCREMENT PRIMARY KEY,
                       compartments INT,
                       capacity INT
                        occupants INT)
